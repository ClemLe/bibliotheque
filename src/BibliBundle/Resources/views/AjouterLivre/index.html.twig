{% extends '::base.html.twig' %}

{% block body %}
    
<div class="container">
    <div class="row">
        
        {% if codeErreur == 1 %}
        <div id="messageUser">
            <div class="alert alert-success alert-dismissible">
                <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
               <strong>Merci !</strong> Le livre a bien été ajouté !
            </div>
        </div>
        {% endif %} 
       <div class="wrapper">
       
                <form class="form-horizontal" action="{{ path('bibli_ajouter_livre') }}" method="post">
                       
                        <form action="#" method="post" {{ form_enctype(form) }}>
                            {{ form_widget(form) }}
                        </form>
                        
                        <button type="submit" name='action'class="btn btn-lg btn-primary btn-block" id="btnContactUs">Ajouter le livre</button>
               
                </form>
       </div>

   
 
 {% endblock%}